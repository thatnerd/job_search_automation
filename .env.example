# LinkedIn Credentials
LINKEDIN_EMAIL=your_email@example.com
LINKEDIN_PASSWORD=your_password

# Encryption key for cookie storage (generate a strong key)
# You can generate one with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
COOKIE_ENCRYPTION_KEY=your_encryption_key_here

# Optional: Chrome driver path (if not using webdriver-manager)
# CHROME_DRIVER_PATH=/path/to/chromedriver

# Optional: Run browser in headless mode (true/false)
HEADLESS_MODE=false